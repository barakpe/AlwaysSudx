Persistent domain propagation with three-beat RAM snapshots. All 19 solvable course application cases and 4,347 RTL executions pass. Standalone: 45.55 MHz, 24,517 fitted LEs; unweighted mean cycles/Fmax: 6.6936 us, worse than the baseline despite fewer cycles.

The full-system 50 MHz build generated programming files but failed timing (40.98 MHz full-system Fmax). Those failed-clock programming files are not distributed. Lower physical clocks are permitted, but this version also loses the standalone-normalized score, so v2 is preferred.

Assets preserve the exact tagged source and course-generated `qsyn_xlr -all` TGZ. `SHA256SUMS` covers the assets. This is an experimental milestone, not a hardware-ready release.
