Verified first inequality baseline, preserved for comparison. All 19 solvable course application tests pass both checkers. Standalone: 51.77 MHz, 24,875 fitted LEs; unweighted mean application cycles/Fmax: 6.2066 us. Additional RTL regressions are recorded in the repository.

Assets include the exact tagged source and the course-generated `qsyn_xlr -all` TGZ. This baseline has no full-system programming image; use the later validated programming release for hardware. `SHA256SUMS` covers the assets.
